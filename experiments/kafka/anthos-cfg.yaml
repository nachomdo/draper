applySpecVersion: 1
spec:
  configSync:
    enabled: true
    sourceFormat: unstructured
    syncRepo: https://github.com/nachomdo/draper/
    syncBranch: gke-anthos
    secretType: none
    policyDir: experiments/kafka/config

